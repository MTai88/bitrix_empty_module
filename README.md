# Basic example of custom 1C Bitrix module

### Setup

- run in console `./install.sh` enter module name and module namespace or change it manually in module files
- install module via bitrix admin panel page